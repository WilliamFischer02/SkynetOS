/**
 * Superseded on 2026-09-12 by services/voice.ts, which runs voice control rather than only describing it.
 * Kept as a re-export so nothing importing the old path breaks; it can be removed with William's approval.
 */
export { voiceStatus } from './voice.js';
