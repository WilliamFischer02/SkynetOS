Per-agent persona briefs for non-JARVIS chat agents (the Observer, the SITW editor, and so on).
Each is a short markdown file referenced by a node's `persona` field. Keep them under 60 lines:
role, standing rules, what they own, what they never do, session ritual.
