# Away report — 2026-09-11, 15:22 pass (PLAN level)

## What I did

Surveyed the codex index, all four existing project files, `handoff.md`, and `docs/06-ROADMAP.md`,
then cross-checked against `C:/dev`. Found six repos with real, current CLAUDE.md/handoff state
that had **zero presence in the codex** — no JARVIS session had board visibility into them before
today: `BitRunners`, `StackAssembler`, `MCCamOp`, `Just1Nudge`, `wfp-site-ops`, `story-universe-map`.
(`MCCamOp` and `Just1Nudge` had already been phantom-proposed into the `minecraftos` room by an
earlier pass today, but never got a project file or index line.)

Wrote `codex/projects/{bitrunners,stackassembler,mccamop,just1nudge,wfp-site-ops,story-universe-map}.md`,
each with a state summary and a concrete Next section sourced from that repo's own handoff/roadmap
docs, and added index lines for all six under a new "GoobOS" category (for the non-Minecraft Goob
Entertainment Co. projects) and the existing MinecraftOS category. Bumped `codex/index.md`'s
`updated:` date and its "most project files don't exist yet" count from four to ten. Added a short
note to `docs/06-ROADMAP.md` documenting the gap and the fix.

Checked phantoms on every board (`root`, `minecraftos`, `gameos`, `storyos`, `deductionos`): all
five are already at the 4-per-room cap, all proposed by an earlier away-mode pass today
(~09:02–10:35). Nothing left to propose — no board recommendation from this pass.

## What needs William's decision

1. **BitRunners has an unmerged PR sitting since 2026-07-12** (`main...claude/mega3-2026-07-11`,
   twelve commits, all gates green) — `gh` was unauthed in that session so no PR ever opened. This
   is over two months old and was invisible to JARVIS until this pass. Worth opening the PR and
   applying the authored-not-applied migration 0019.
2. Everything already flagged in `docs/06-ROADMAP.md`'s "Right now" section from the earlier
   09:02–10:35 pass today is still the standing priority: restart SkynetOS and click through
   today's uncommitted batches (forks A–Q) — none of it has been seen on screen yet.
3. The four already-proposed phantom sets (4 per room × 5 boards, all from the earlier pass) are
   waiting for tick/cross review whenever you're back at the board.

## What I did not touch

No commits, no pushes, no board edits beyond reading, no session starts. Only wrote under
`codex/` and to `docs/06-ROADMAP.md`, per this session's bounds.
